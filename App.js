const App = () => {
    return (
        <div>
            <h2>Welcome to Kansas Policy Prescriptions</h2>
            <p>This is a test to see if React is working.</p>
        </div>
    );
};

ReactDOM.render(<App />, document.getElementById('root'));
